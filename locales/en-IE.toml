_version = 1

[errors]
serialise = "serialisation error: could not serialise data to %{which} (while %{context})"
deserialise = "deserialisation error: could not deserialise data from %{which} (while %{context})"

[warnings]
toml_all = "the above is not a list of valid recipe files—do not attempt to deserialise it"
